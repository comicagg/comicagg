<?xml version="1.0" encoding="UTF-8" ?>
<strip id="{{ history.id }}" imageurl="{{ history.url }}" imagetext="{{ history.alt_text|default:"" }}" date="{{ history.date|date:"r" }}" timestamp="{{ history.date|date:"U" }}"/>
