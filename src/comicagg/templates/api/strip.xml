<?xml version="1.0" encoding="UTF-8" ?>
<strip id="{{ strip.id }}" imageurl="{{ strip.url }}" imagetext="{{ strip.alt_text|default:"" }}" date="{{ strip.date|date:"r" }}" timestamp="{{ strip.date|date:"U" }}"/>
